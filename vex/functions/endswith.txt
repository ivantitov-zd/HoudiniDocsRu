= endswith =

#type: vex
#context: all
#group: string

"""Возвращает 1, если строка оканчивается указанной строкой."""

:usage: `int endswith(const string haystack, const string needle)`

:returns:
    Возвращает `1`, если строка оканчивается указанной строкой, иначе возвращает `0`.

:bubble:
    #dir: right
    #display: gray
    [English version of this page|http://www.sidefx.com/docs/houdini/vex/functions/endswith]

@related

- [Vex:startswith]
